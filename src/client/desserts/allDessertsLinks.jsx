import React from 'react';
import { Link } from 'react-router-dom';

const AllDessertsLinks = (props) => (
  <div>
    <Link to="/dessert/Matcha-Soft-Serve">Matcha Soft Serve</Link>
  </div>
);

export default AllDessertsLinks;
